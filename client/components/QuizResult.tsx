function QuizResult() {
 
const correctCheck = arr.map(id => {
  id.answer
  let score = [0]
    if (id.answer === correct){
      score++
    }
  else 
  })
 
 
///all variables below this point are filler only. Do not use
 return (
  <div>
    <h1>{`${quizId}`}</h1>
    <p>You answered {`${correct}`} out of {`${totalQuestions}`} questions correctly and scored {`${calcAmount}`}%</p>
  </div>
)}

export default QuizResult
//get correct_answer
// if correct_answer = true then add 1 point to a points object
// map through potential entire array of answers and add up points in object
// use math prob division to work out a percentage. divide correct answer by no. of answers?
// display these results on the page for the user to look at.